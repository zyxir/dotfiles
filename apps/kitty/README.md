These files configure kitty's behavior.
- `kitty.conf`: kitty's main configuration file.
- `dark-theme.auto.conf`: theme used when the OS uses a light theme.
- `light-theme.auto.conf`: theme used when the OS uses a dark theme.
- `no-preference-theme.auto.conf`: theme used when the OS has no light/dark preference.